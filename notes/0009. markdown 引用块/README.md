# [0009. markdown 引用块](https://github.com/tnotesjs/TNotes.markdown/tree/main/notes/0009.%20markdown%20%E5%BC%95%E7%94%A8%E5%9D%97)

<!-- region:toc -->

- [1. 🎯 本节内容](#1--本节内容)
- [2. 🫧 评价](#2--评价)
- [3. 🤔 如何在 markdown 中创建引用块？](#3--如何在-markdown-中创建引用块)
- [4. 🤔 如何创建多行引用块？](#4--如何创建多行引用块)
- [5. 🤔 如何创建嵌套引用块？](#5--如何创建嵌套引用块)
- [6. 🤔 引用块中可以使用其他 markdown 语法吗？](#6--引用块中可以使用其他-markdown-语法吗)
- [7. 🤔 引用块前后需要空行吗？](#7--引用块前后需要空行吗)
- [8. 🤔 如何在引用块中标注引用来源？](#8--如何在引用块中标注引用来源)

<!-- endregion:toc -->

## 1. 🎯 本节内容

- todo

## 2. 🫧 评价

- todo

## 3. 🤔 如何在 markdown 中创建引用块？

在段落开头使用 `>` 加空格：

```
> 这是一段引用内容。
```

`>` 符号后的空格不是强制要求，但加上空格更符合规范且可读性更好。

## 4. 🤔 如何创建多行引用块？

方式一：每行都以 `>` 开头：

```
> 第一行引用内容。
> 第二行引用内容。
> 第三行引用内容。
```

方式二：只在段落首行加 `>`，其余行直接续写（懒惰写法，部分解析器支持）：

```
> 第一行引用内容。
第二行引用内容（懒惰续行）。
```

推荐每行都添加 `>`，以保证在所有解析器中表现一致。

## 5. 🤔 如何创建嵌套引用块？

使用多个 `>` 符号实现嵌套，每增加一层嵌套就多加一个 `>`：

```
> 外层引用内容。
>
> > 内层嵌套引用内容。
> >
> > > 更深一层的引用。
```

嵌套引用常用于表示回复或对话场景（如邮件引用）。

## 6. 🤔 引用块中可以使用其他 markdown 语法吗？

可以。引用块内部支持大多数 markdown 语法，包括标题、列表、加粗、代码等：

```
> ## 引用中的标题
>
> - 列表项一
> - 列表项二
>
> 这里有**加粗**和 `行内代码`。
```

利用这一特性可以在引用块中构建结构完整的内容块。

## 7. 🤔 引用块前后需要空行吗？

引用块前后建议各留一个空行，以确保解析器将其正确识别为独立的引用块，而不是与前后段落合并：

```
普通段落内容。

> 引用块内容。

继续普通段落内容。
```

连续的引用块行（即使中间没有空行）通常会被解析为同一个引用块。若要分割为两个独立引用块，需在两者之间插入一个非引用行（如空行或普通段落）。

## 8. 🤔 如何在引用块中标注引用来源？

markdown 本身没有专门的引用来源语法，通常的做法是在引用块内容末尾用 `—` 加来源信息标注，或在引用块外紧接一行说明文字：

```
> 我们所知道的一切，都只是可能性。
> — 理查德·费曼

> Stay hungry, stay foolish.

— 史蒂夫·乔布斯，斯坦福大学毕业典礼演讲，2005
```

具体排版风格因平台和个人习惯而异，保持一致即可。
